spring:
  config:
    activate:
      on-profile: docker

  datasource:
    url: jdbc:postgresql://fintech-postgres:5432/fintech_db
    username: fintech
    password: fintech

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
